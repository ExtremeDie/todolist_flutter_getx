include: package:lints/recommended.yaml

analyzer:
  exclude: 
    - [build/**]
    - "**/*.g.dart"
    - "**/*.freezed.dart"
  errors:
    invalid_annotation_target: ignore

linter:
  rules:
    unnecessary_overrides: false